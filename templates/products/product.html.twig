{% extends 'base.html.twig' %}

{% block body %}
    <section class="clearfix">
        {% if product %}
            <table id="products" class="table table-striped">
                <thead>
                <tr>
                    <th>Назва товару</th>
                    <th>Категорiя</th>
                    <th>Цiна</th>
                    <th>Опис</th>
                </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>{{ product.name }}</td>
                        <td> {{ category }} </td>
                        <td> {{ product.price }} </td>
                        <td> {{ product.description }} </td>
                    </tr>
                </tbody>
            </table>
        {% else %}
            <p>Товар {{ productId }} не знайдено</p>
        {% endif %}
    </section>
{% endblock %}